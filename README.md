cài đặt nodejs và vs code, live server


   ```bash
   cd backend
   ```bash
   npm install
   ```
   ```env
   PORT=5000
   DB_USER=sa
   DB_PASSWORD=[PASSWORD]
   DB_SERVER=localhost
   DB_DATABASE=QLDSV_HTC
   CORS_ORIGIN=http://127.0.0.1:5500
   ```
   ```bash
   npm run dev
   ```

vao extension cai dat live server xong chuot phai vao index.html ->open with live server