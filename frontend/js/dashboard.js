/* ====================================
   DASHBOARD LOGIC
   File: js/dashboard.js
==================================== */

document.addEventListener('pageLoaded', (e) => {
  if (e.detail.pageId === 'dashboard') {
    // Logic cho dashboard.html sẽ viết ở đây
    console.log("Dashboard loaded");
  }
});
